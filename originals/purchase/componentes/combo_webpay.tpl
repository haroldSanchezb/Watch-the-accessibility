			<select name="cuotas" class="combo-box" {DISABLED}>
				{OPCIONES_CUOTAS}
			</select>
			<input type="hidden" name="webpay_precio_contado" id="webpay_precio_contado" value="{PRECIO_CONTADO}">
			<input type="hidden" name="webpay_moneda" id="webpay_moneda" value="{MONEDA}">
